---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint ../splunk_all
  ansible-lint ../splunk_all
  flake8 ../splunk_all --exit-zero
platforms:
  - name: instance
    image: centos:7
provisioner:
  name: ansible
verifier:
  name: testinfra
