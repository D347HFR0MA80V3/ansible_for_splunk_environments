---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint ../splunk_all
  ansible-lint ../splunk_all
  flake8 ../splunk_all
platforms:
  - name: centos
    image: centos:7
    privileged: true
    security_options:
      - seccomp=unconfined
  - name: ubuntu
    image: ubuntu:16.04
    privileged: true
    security_options:
      - seccomp=unconfined
provisioner:
  name: ansible
  ansible_args:
    - --extra-vars=role_name=splunk_all
  playbooks:
    prepare: ../../../../molecule_shared/playbooks/prepare.yml
    converge: ../../../../molecule_shared/playbooks/converge.yml
verifier:
  name: testinfra
  env:
    TESTVARS_EXTRA_VARS: ../../defaults/main.yml
