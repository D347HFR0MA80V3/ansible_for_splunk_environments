---
language: python
python:
  - "3.6"
services:
  - docker
install:
  - pip install molecule docker
  - pip install ansible-lint
script:
  - molecule --version
  - ansible --version
  - cd roles/splunk_all && molecule test
